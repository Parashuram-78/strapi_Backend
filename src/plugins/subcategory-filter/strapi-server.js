'use strict';

/**
 * Simple subcategory-filter plugin
 */
module.exports = () => ({
    bootstrap() {
        // No bootstrap code needed
    },

    register() {
        // No register code needed - we're implementing filtering directly in the controller
    }
}); 